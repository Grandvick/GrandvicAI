/**
 * Placeholder database types.
 *
 * Once you have a real Supabase project connected, generate accurate types
 * with the Supabase CLI and replace this file's contents:
 *
 *   npx supabase gen types typescript --project-id <your-project-id> > src/lib/supabase/types.ts
 *
 * Until then, we type the client loosely so the app compiles and runs
 * against an empty or partially-migrated database without false errors.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
