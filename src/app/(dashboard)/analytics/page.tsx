import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      phase={9}
      description="Sales, marketing, recruitment and customer-service analytics — built from real data only, never fabricated — are built in Phase 9."
    />
  );
}
