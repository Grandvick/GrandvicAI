import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Content Calendar"
      phase={7}
      description="A visual calendar for drafting, approving, scheduling and publishing content is built in Phase 7 (Content Factory)."
    />
  );
}
