import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function JobsPage() {
  return (
    <ComingSoon
      title="Jobs Abroad Opportunities"
      phase={2}
      description="Creating, publishing, and expiring job opportunities — with automatic protection so closed jobs are never shown to customers — is built in Phase 2."
    />
  );
}
