import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function AiCommandCentrePage() {
  return (
    <ComingSoon
      title="AI Command Centre"
      phase={3}
      description="A natural-language command interface, backed by controlled tools (not raw database access) and guardrails on high-impact actions, is built starting Phase 3 (AI Core)."
    />
  );
}
