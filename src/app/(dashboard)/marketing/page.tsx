import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function MarketingPage() {
  return (
    <ComingSoon
      title="Content Factory"
      phase={7}
      description="AI-assisted post, caption and poster-copy generation — always from verified business data, never invented — is built in Phase 7."
    />
  );
}
