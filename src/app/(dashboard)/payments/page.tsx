import { ComingSoon } from "@/components/dashboard/ComingSoon";

export default function PaymentsPage() {
  return (
    <ComingSoon
      title="Payments"
      phase={9}
      description="Payment tracking is built once a payment provider is selected together with the business owner — see the Payments section of ARCHITECTURE.md."
    />
  );
}
